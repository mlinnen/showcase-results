# Orchestration Log — Bilbo (2026-04-23)

**Agent:** Bilbo (Data Engineer)  
**Mode:** background  
**Session:** Issue #30 JSON-Layer Revision

## Mandate

Revise issue #30 after user changed direction from Joomla filtering to JSON generation. Move the checked-in carver filtering from the presentation layer (Joomla rendering) into the data generation layer (CLI JSON output).

## Outcome

✅ **COMPLETED**

- Updated `src/ShowcaseResults.Cli/Parsing/SpreadsheetParser.cs` to detect explicit checked-in signal in `Competitor.xlsx`
- Implemented fallback in `src/ShowcaseResults.Cli/Program.cs`: if no explicit checked-in column exists, filter competitors to only those referenced by assigned prizes or ranked judging results before JSON serialization
- Updated schema (`schema/results.schema.json`), README (`README.md`), and Joomla docs (`docs/joomla-extension.md`) to describe `competitors` as checked-in-only for all downstream consumers
- Regenerated sample JSON (`joomla/com_showcaseresults/media/data/results-2026T.json`) with 35 checked-in competitors (excluded IDs 22, 34 from 37 registrations)
- Downstream contract: Joomla `ResultsService.php` now trusts the filtered JSON data; no redundant filtering needed at render time

## Branch & PR

- **Branch:** `squad/30-only-show-checked-in-carvers-in-list`
- **PR:** #33 https://github.com/mlinnen/showcase-results/pull/33

## Key Decisions

- **Data-layer ownership:** Filtering moved upstream to the source of truth (CLI JSON generation)
- **Workbook constraint:** Current data has no explicit checked-in column; fallback uses result-bearing carver IDs as the signal
- **Consumer trust:** Joomla component simplified to consume pre-filtered JSON

## Next Steps

- Await PR #33 review and merge
- Verify Joomla carvers list displays 35 competitors correctly on production deployment
